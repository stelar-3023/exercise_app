const toggleLog = require("../LogModal");

describe("The toggleLog function should open the Log Modal", () => {
  it("toggleLog function exists", () => {
    expect(toggleLog).toBeDefined();
  });
});
